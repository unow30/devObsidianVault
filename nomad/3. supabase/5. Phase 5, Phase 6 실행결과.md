## 📊 개요
Phase 5(프론트엔드 데이터 연동)와 Phase 6(인터랙티브 기능)의 구현이 완료되었습니다.
## Phase 5: 프론트엔드 데이터 연동 ✅
### 1️⃣ API 통합
생성된 API 엔드포인트 (9개)

| 엔드포인트                                   | 메서드    | 기능               |
| --------------------------------------- | ------ | ---------------- |
| `/api/cities`                           | GET    | 도시 목록 (필터/정렬)    |
| `/api/cities/[slug]`                    | GET    | 도시 상세 정보         |
| `/api/cities/[slug]/metrics`            | GET    | 도시 메트릭 (기후, AQI) |
| `/api/cities/[slug]/ratings`            | POST   | 평가 제출            |
| `/api/cities/[slug]/ratings`            | PUT    | 평가 수정            |
| `/api/cities/[slug]/ratings`            | DELETE | 평가 삭제            |
| `/api/cities/[slug]/reviews`            | POST   | 리뷰 작성            |
| `/api/cities/[slug]/reviews/[reviewId]` | PUT    | 리뷰 수정            |
| `/api/cities/[slug]/reviews/[reviewId]` | DELETE | 리뷰 삭제            |


### 파일 생성: app/api/ 디렉토리 구조
  ```
  app/api/
  ├── cities/
  │   ├── route.ts                    # GET /api/cities
  │   └── [slug]/
  │       ├── route.ts                # GET /api/cities/[slug]
  │       ├── metrics/
  │       │   └── route.ts            # GET /api/cities/[slug]/metrics
  │       ├── ratings/
  │       │   └── route.ts            # POST/PUT/DELETE ratings
  │       └── reviews/
  │           ├── route.ts            # POST reviews
  │           └── [reviewId]/
  │               └── route.ts        # PUT/DELETE reviews
  ```
### 2️⃣ 데이터 베이스 쿼리 라이브러리
  파일: lib/supabase-queries.ts (생성)

  주요 함수:
  - getCitiesList(filters, sort, page)     // 필터/정렬된 도시 목록
  - getCityBySlug(slug)                     // 단일 도시 상세 조회
  - convertDatabaseCityToCity()              // DB 스키마 → App 타입 변환
  - Rating CRUD: createRating, updateRating, deleteRating
  - Review CRUD: createReview, updateReview, deleteReview

### 3️⃣ 프론트엔드 컴포넌트 수정
  수정된 파일

| 파일                          | 주요 변경사항                          | 영향 |
|-------------------------------|---------------------------------------|------|
| app/page.tsx                 | Mock → /api/cities async 호출[S]      | 홈페이지 동적 로드 [3] |
| components/filters/FilterBar.tsx | URL sync + useTransition             | 필터 UX 개선 |
| app/city/[citySlug]/page.tsx | getCityBySlug API + 리뷰 10개 로드    | 상세 페이지 완성 |

  
  FilterBar 필터 옵션:
  - region: "All" | "Seoul" | "Busan" | "Jeju" | ... (15개 도시)
  - budget: "All" | "Low" | "Mid" | "High"
  - sort: "popular" | "rating" | "cost" | "temp"

  ---
## Phase 6: 인터랙티브 기능 ✅

### 1️⃣ 좋아요/싫어요 시스템
파일: components/city/LikeDislikeButtons.tsx (수정)
기능:
 Props:
  - likes: number                // 좋아요 수
  - dislikes: number             // 싫어요 수
  - cityId?: string              // API 제출용
동작:
  1. cityId 없음 → 로컬 상태만 관리 (UI 피드백)
  2. cityId 있음 → API 호출 (/api/cities/{cityId}/ratings)
  3. 401 응답 → 로그인 다이얼로그 표시
  4. 제출 중 → 로딩 스피너 표시

### 2️⃣ 평가 제출 폼

파일: components/city/RatingForm.tsx (신규)

  UI:
  ```
  ┌─────────────────────────────┐
  │ 도시명에 평가하기           │
  │ 별점과 감정을 함께 남겨주세요 │
  ├─────────────────────────────┤
  │ ★ ★ ★ ★ ★  (5점)           │
  │                             │
  │ [😞 싫어요] [😐 중립] [😊 좋아요] │
  └─────────────────────────────┘

  ```
  
  기능:
  - 별점: 1~5점 선택 (호버 효과)
  - 감정: 3가지 옵션 (-1, 0, 1)
  - API: POST /api/cities/{cityId}/ratings
  - 인증: 401 시 로그인 다이얼로그

### 3️⃣ 리뷰 작성 폼
파일: components/city/ReviewForm.tsx (신규)

  UI:
  ```
  ┌──────────────────────────────┐
  │ 도시명에 리뷰 작성하기        │
  │ 당신의 경험을 공유해주세요     │
  ├──────────────────────────────┤
  │ 별점: ★ ★ ★ ★ ★  (5점)      │
  │                              │
  │ 리뷰 제목                     │
  │ [예: 좋은 날씨와...]          │
  │                              │
  │ 리뷰 내용                     │
  │ [상세 설명...]               │
  │                              │
  │ [취소] [리뷰 제출하기]        │
  └──────────────────────────────┘
  ```
  
  입력 필드:
  - title (필수): 리뷰 제목, 최대 100자
  - content (필수): 리뷰 내용, 최대 1000자
  - rating (필수): 1~5 별점

  폼 검증:
  ```
  if (!title.trim()) → "제목을 입력해주세요"
  if (!content.trim()) → "리뷰 내용을 입력해주세요"
  if (rating === 0) → "별점을 선택해주세요"
  ```
  
### 4️⃣ 리뷰 섹션 통합
  파일: components/city/ReviewSection.tsx (수정)

  변경 사항:
  // 기존: 정렬 기능만 제공
  // 변경: RatingForm, ReviewForm 컴포넌트 추가

  리뷰 없을 때:
  - 메시지: "아직 리뷰가 없습니다"
  - 버튼: RatingForm, ReviewForm

  리뷰 있을 때:
  - 정렬 옵션: 최신순, 평점순, 좋아요순
  - 리뷰 카드 목록
  - 하단: RatingForm, ReviewForm 버튼

### 5️⃣ UI 컴포넌트 라이브러리

  신규 생성 파일:
  ```
  ┌────────────────────────────┬─────────────────────────┐
  │            파일            │          설명           │
  ├────────────────────────────┼─────────────────────────┤
  │ components/ui/dialog.tsx   │ Radix UI Dialog 래퍼    │
  ├────────────────────────────┼─────────────────────────┤
  │ components/ui/textarea.tsx │ 텍스트에어리어 컴포넌트 │
  └────────────────────────────┴─────────────────────────┘
  ```

  ---
### 📦 의존성 추가

  설치된 패키지:
  npm install @radix-ui/react-dialog

  package.json 변경:
  ```
  {
    "dependencies": {
      "next": "15.5.9",
      "react": "^19.0.0-rc",
      "typescript": "^5",
      "@supabase/supabase-js": "^2.43.4",
      "@supabase/ssr": "^0.0.10",
      "@radix-ui/react-dialog": "^1.1.2",  // ← 새로 추가
      "lucide-react": "^0.344.0",
      "tailwind-css": "^3.3.0"
    }
  }
  ```
   ---
### 🏗️ 데이터베이스 스키마

  생성된 테이블:
  ```
  -- 1. city_metrics (도시 메트릭)
  CREATE TABLE city_metrics (
    id UUID PRIMARY KEY,
    city_id UUID REFERENCES cities,
    month INT,
    avg_temp DECIMAL,
    avg_humidity INT,
    avg_aqi INT,
    avg_internet_speed INT,
    created_at TIMESTAMP
  );

  -- 2. user_ratings (사용자 평가)
  CREATE TABLE user_ratings (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users,
    city_id UUID REFERENCES cities,
    overall_score INT (1-5),
    likes INT (-1, 0, 1),  -- dislike, neutral, like
    created_at TIMESTAMP,
    UNIQUE(user_id, city_id)
  );

  -- 3. user_reviews (사용자 리뷰)
  CREATE TABLE user_reviews (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users,
    city_id UUID REFERENCES cities,
    title VARCHAR(100),
    content TEXT,
    rating INT (1-5),
    likes INT (좋아요 수),
    stay_duration VARCHAR,
    created_at TIMESTAMP
  );

  -- 4. users (사용자 프로필)
  CREATE TABLE users (
    id UUID PRIMARY KEY,
    email VARCHAR UNIQUE,
    display_name VARCHAR,
    avatar_url VARCHAR,
    created_at TIMESTAMP
  );
  ```
  

  RLS 정책:
  - ✅ 공개 읽기: cities, city_metrics
  - ✅ 인증 필수 쓰기: user_ratings, user_reviews
  - ✅ 본인만 수정/삭제: user_reviews, user_ratings

  ---
### 📝 문서화

생성된 파일: API_DOCUMENTATION.md

포함 내용:
  - 9개 API 엔드포인트 상세 명세
  - 요청/응답 예제
  - 에러 처리 가이드
  - 인증 방식

  ---
### 🧪 테스트 결과
```
┌───────────────────┬─────────────────────────────┐
  │       항목        │            상태             │
  ├───────────────────┼─────────────────────────────┤
  │ TypeScript 컴파일 │ ✅ 성공 (0 에러)            │
  ├───────────────────┼─────────────────────────────┤
  │ 개발 서버 시작    │ ✅ 정상 실행                │
  ├───────────────────┼─────────────────────────────┤
  │ 홈페이지 로드     │ ✅ API에서 도시 목록 로드   │
  ├───────────────────┼─────────────────────────────┤
  │ 도시 상세 페이지  │ ✅ API에서 상세 정보 로드   │
  ├───────────────────┼─────────────────────────────┤
  │ 필터/정렬         │ ✅ URL 쿼리 파라미터 동기화 │
  └───────────────────┴─────────────────────────────┘
```

---
### 🚀 현재 상태

  ✅ Phase 1-6 모두 완료
  ✅ 개발 서버 정상 실행 (http://localhost:3000)
  ✅ Supabase 데이터베이스 연동 완료
  ✅ 사용자 인증 기능 작동
  ✅ 평가/리뷰 시스템 완성

  ---
### 🎯 기능 요약

  사용자는 이제 다음을 할 수 있습니다:
  ```
    ┌────────────────────┬───────────────────────┐
  │        기능        │         상태          │
  ├────────────────────┼───────────────────────┤
  │ 도시 목록 조회     │ ✅ 필터/정렬 가능     │
  ├────────────────────┼───────────────────────┤
  │ 도시 상세 정보     │ ✅ 메트릭, 차트 포함  │
  ├────────────────────┼───────────────────────┤
  │ 도시 좋아요/싫어요 │ ✅ 별도 버튼          │
  ├────────────────────┼───────────────────────┤
  │ 평가 제출          │ ✅ 별점 + 감정        │
  ├────────────────────┼───────────────────────┤
  │ 리뷰 작성          │ ✅ 제목 + 내용 + 별점 │
  ├────────────────────┼───────────────────────┤
  │ 리뷰 조회          │ ✅ 정렬 기능 포함     │
  ├────────────────────┼───────────────────────┤
  │ 로그인 필요        │ ✅ 401 시 자동 처리   │
  └────────────────────┴───────────────────────┘
  ```

  ---
### 🔧 다음 단계 (선택사항)

  1. 실시간 업데이트 - Supabase Realtime으로 평가/리뷰 즉시 반영
  2. 검색 기능 - 도시명으로 검색
  3. 사용자 프로필 - 작성한 리뷰 관리
  4. 프로덕션 배포 - Vercel에 배포
  5. 성능 최적화 - 이미지 최적화, 캐싱 전략