
## Phase 1: 평점 시스템 개선 - 별점 삭제 및 좋아요/싫어요 버튼 추가  
  
### 오버뷰  
도시 카드의 별점 평점 시스템(⭐ overallScore)을 제거하고, 좋아요/싫어요 버튼을 인터랙티브하게 변경합니다. 사용자가 버튼을 클릭하면 상태에 따라 아이콘 색상이 변경되고 숫자가 증가/감소합니다.  
  
### 수정/개선 기능  
- [x] `components/city/CityCard.tsx` 파일에서 별점 표시 부분 제거 (58-67라인)  
- [x] 좋아요/싫어요 부분(94-103라인)을 버튼으로 변경  
- [x] 좋아요/싫어요 상태를 관리하는 클라이언트 컴포넌트로 전환  
- [x] 좋아요 버튼 클릭 시:  
  - [x] 아직 선택하지 않았으면: 좋아요 활성화 (파란색), likes +1  
  - [x] 이미 좋아요 상태면: 좋아요 취소 (회색), likes -1  
  - [x] 이미 싫어요 상태면: 좋아요로 변경 (파란색), likes +1, dislikes -1  
- [x] 싫어요 버튼 클릭 시:  
  - [x] 아직 선택하지 않았으면: 싫어요 활성화 (빨간색), dislikes +1  
  - [x] 이미 싫어요 상태면: 싫어요 취소 (회색), dislikes -1  
  - [x] 이미 좋아요 상태면: 싫어요로 변경 (빨간색), dislikes +1, likes -1  
- [x] `types/city.ts`에서 `overallScore` 필드 타입 정의는 유지 (데이터 마이그레이션 고려)  
- [ ] 필요시 별점 기반 정렬 옵션을 좋아요순으로 변경  
  
### 작업 완료 후 검증  
- [x] 도시 카드에 별점(⭐)이 표시되지 않는지 확인  
- [x] 좋아요 버튼 클릭 시 파란색으로 변경되는지 확인  
- [x] 싫어요 버튼 클릭 시 빨간색으로 변경되는지 확인  
- [x] 좋아요 → 싫어요 전환 시 숫자가 정확히 변경되는지 확인  
- [x] 싫어요 → 좋아요 전환 시 숫자가 정확히 변경되는지 확인  
- [x] 버튼 재클릭 시 선택 취소가 정상 작동하는지 확인  
- [x] 모바일/태블릿/데스크톱 모든 해상도에서 정상 작동 확인  
  
### 주요 파일  
- [x] `components/city/CityCard.tsx` - 카드 UI 수정  
- [x] `components/city/LikeDislikeButtons.tsx` - 좋아요/싫어요 인터랙션 컴포넌트 생성  
- [x] `types/city.ts` - 타입 확인  
- [ ] `types/city.ts` SORT_OPTIONS - 정렬 옵션 수정 (선택사항)  
  
### 구현 가이드  
- [x] CityCard를 클라이언트 컴포넌트로 변경 (`'use client'` 추가) - LikeDislikeButtons만 클라이언트 컴포넌트로 분리  
- [x] `useState`를 사용하여 현재 사용자의 선택 상태 관리 (`'none' | 'like' | 'dislike'`)  
- [x] `useState`를 사용하여 현재 likes, dislikes 숫자 관리  
- [x] ThumbsUp/ThumbsDown 아이콘을 Button으로 감싸기  
- [x] 조건부 스타일링으로 색상 변경 (활성화: text-blue-500/text-red-500, 비활성화: text-muted-foreground)  
- [x] 클릭 핸들러에서 상태 전환 로직 구현  
  
---  
  
## Phase 2: 네비게이션 간소화 - 불필요한 버튼 제거  
  
### 오버뷰  
Header 컴포넌트에서 다른 페이지로 이동하는 버튼들을 제거하여 UI를 간소화합니다. 로고, 검색창, 로그인/회원가입 버튼만 남깁니다.  
  
### 수정/개선 기능  
- [x] `components/layout/Header.tsx` 파일 검토  
- [x] 페이지 이동 버튼이 있는지 확인 (현재는 없을 수 있음)  
- [x] 만약 추가 네비게이션 링크가 있다면 제거  
- [x] 로고, 검색창, 로그인/회원가입 버튼만 유지  
- [x] 모바일 메뉴가 있다면 간소화  
  
### 작업 완료 후 검증  
- [x] Header에 로고가 정상 표시되는지 확인  
- [x] 검색창이 데스크톱과 모바일에서 정상 표시되는지 확인  
- [x] 로그인/회원가입 버튼만 존재하는지 확인  
- [x] 불필요한 네비게이션 링크가 모두 제거되었는지 확인  
- [x] 모든 해상도에서 레이아웃이 깨지지 않는지 확인  
  
### 주요 파일  
- [x] `components/layout/Header.tsx` - 헤더 컴포넌트  
  
### 구현 가이드  
- [x] Header.tsx 파일에서 불필요한 Link 컴포넌트 제거  
- [x] 네비게이션 메뉴나 버튼 그룹이 있다면 삭제  
- [x] 레이아웃이 깨지지 않도록 flex 구조 유지  
  
---  
  
## Phase 3: 필터 시스템 재설계 - 새로운 필터 옵션 구현  
  
### 오버뷰  
FilterBar 컴포넌트의 필터 옵션을 사용자 요구사항에 맞게 재설계합니다. 예산, 지역, 환경, 최고계절 4가지 필터를 제공합니다.  
  
### 수정/개선 기능  
- [x] `types/city.ts` 파일에 새로운 필터 타입 추가:  
  - [x] `BudgetRange`: "under100" | "100to200" | "over200"  
  - [x] `Region`: "all" | "seoul" | "gyeongsang" | "jeolla" | "gangwon" | "jeju" | "chungcheong"  
  - [x] `Environment`: "nature" | "urban" | "cafe" | "coworking"  
  - [x] `BestSeason`: "spring" | "summer" | "fall" | "winter"  
- [x] 각 필터에 대한 옵션 상수 정의:  
  - [x] `BUDGET_OPTIONS`: 100만원 이하, 100~200만원, 200만원 이상  
  - [x] `REGION_OPTIONS`: 전체, 수도권, 경상도, 전라도, 강원도, 제주도, 충청도  
  - [x] `ENVIRONMENT_OPTIONS`: 자연친화, 도심선호, 카페작업, 코워킹 필수  
  - [x] `SEASON_OPTIONS`: 봄, 여름, 가을, 겨울  
- [x] `components/filters/FilterBar.tsx` 수정:  
  - [x] 기존 필터 제거  
  - [x] 4개의 새로운 Select 드롭다운 추가  
  - [x] 상세필터 버튼은 유지 (향후 확장용)  
  - [x] 뷰 전환 버튼 유지  
- [x] 클라이언트 컴포넌트로 변경하여 필터 상태 관리  
  
### 작업 완료 후 검증  
- [x] 예산 필터에 3가지 옵션이 표시되는지 확인  
- [x] 지역 필터에 7가지 옵션이 표시되는지 확인  
- [x] 환경 필터에 4가지 옵션이 표시되는지 확인  
- [x] 최고계절 필터에 4가지 옵션이 표시되는지 확인  
- [x] 각 필터 선택 시 상태가 정상적으로 변경되는지 확인  
- [x] 모바일에서 필터들이 정상적으로 배치되는지 확인  
- [x] 기존 정렬 옵션이 제거되었는지 확인  
  
### 주요 파일  
- [x] `types/city.ts` - 필터 타입 정의  
- [x] `components/filters/FilterBar.tsx` - 필터 UI  
  
### 구현 가이드  
- [x] 필터 타입과 옵션 상수를 먼저 정의  
- [x] FilterBar를 `'use client'`로 변경  
- [x] 각 필터의 선택 상태를 useState로 관리  
- [x] Select 컴포넌트를 사용하여 4개의 드롭다운 구성  
- [x] flex-wrap을 활용하여 반응형 레이아웃 구성  
  
---  
  
## Phase 4: 카드 구조 변경 - 필터 정보 표시  
  
### 오버뷰  
도시 카드의 정보 표시 구조를 변경하여 Phase 3에서 정의한 필터 속성(예산, 지역, 환경, 최고계절)을 key-value 형태로 표시합니다.  
  
### 수정/개선 기능  
- [x] `types/city.ts`의 City 인터페이스에 새 필드 추가:  
  - [x] `budget?: BudgetRange` - 예산 범위 (선택사항)  
  - [x] `environment?: Environment[]` - 환경 특성 (복수 선택 가능, 선택사항)  
  - [x] `bestSeason?: BestSeason` - 최고 계절 (선택사항)  
- [x] `components/city/CityCard.tsx` 카드 레이아웃 수정:  
  - [x] 기존 지표 표시 부분 유지  
  - [x] 새로운 정보 표시 영역 추가 (border-t로 구분)  
  - [x] key-value 형식으로 표시: "예산: 100만원 이하" 등  
- [x] 각 필터 항목에 적절한 이모지 추가:  
  - [x] 예산: 💰  
  - [x] 환경: 🌳  
  - [x] 최고계절: 🌸/☀️/🍂/❄️ (계절별 동적 표시)  
  
### 작업 완료 후 검증  
- [x] 카드에 "예산" 정보가 표시되는지 확인  
- [x] 카드에 "환경" 정보가 표시되는지 확인 (복수 표시 가능)  
- [x] 카드에 "최고계절" 정보가 표시되는지 확인  
- [x] key-value 형식이 읽기 쉽게 표시되는지 확인  
- [x] 카드 전체 레이아웃이 깨지지 않는지 확인  
- [x] TypeScript 컴파일 에러가 없는지 확인  
- [x] ESLint 에러가 없는지 확인  
  
### 주요 파일  
- [x] `types/city.ts` - City 인터페이스 확장  
- [x] `components/city/CityCard.tsx` - 카드 UI 수정  
- [x] `lib/utils.ts` - 필터 값을 한글 레이블로 변환하는 유틸 함수 추가  
  
### 구현 가이드  
- [x] City 타입에 budget, environment, bestSeason 필드 추가 (선택사항으로)  
- [x] 카드 하단부에 새로운 정보 섹션 추가 (border-t로 구분)  
- [x] flex 레이아웃으로 key-value 쌍 배치  
- [x] 각 항목에 적절한 이모지와 텍스트 스타일 적용  
- [x] environment는 배열이므로 map으로 순회하여 표시  
- [x] lib/utils.ts에 유틸 함수 추가: getBudgetLabel, getEnvironmentLabel, getSeasonLabel, getSeasonEmoji  
- [x] 제주시, 서귀포시에 샘플 데이터 추가하여 테스트  
  
---  
  
## Phase 5: 데이터 업데이트 - 필터 속성 추가  
  
### 오버뷰  
`lib/mock-data.ts`의 15개 도시 데이터에 Phase 4에서 정의한 필터 속성(예산, 환경, 최고계절)을 추가합니다. 각 도시별로 최소 하나 이상의 환경 특성을 부여합니다.  
  
### 수정/개선 기능  
- [x] `lib/mock-data.ts` 파일의 각 도시 객체에 필드 추가  
- [x] 각 도시의 `monthlyBudget` 값을 기준으로 `budget` 필드 설정:  
  - [x] 100만원 이하: "under100"  
  - [x] 100~200만원: "100to200"  
  - [x] 200만원 이상: "over200"  
- [x] 각 도시의 특성에 맞는 `environment` 배열 설정:  
  - [x] 제주시, 서귀포, 속초 등: ["nature"]  
  - [x] 부산, 대구, 대전: ["urban", "cafe"]  
  - [x] 강릉, 전주: ["cafe", "nature"]  
  - [x] 세종: ["coworking", "urban"]  
  - [x] 등등 도시 특성에 맞게 배치  
- [x] 각 도시의 `bestSeason` 설정:  
  - [x] 제주, 서귀포: "spring" 또는 "fall"  
  - [x] 강릉, 속초: "summer" (해변)  
  - [x] 부산, 여수: "summer"  
  - [x] 전주: "fall" (단풍)  
  - [x] 대전, 세종: "spring"  
  - [x] 등등 도시 특성에 맞게 배치  
- [x] 15개 도시 모두 필터 속성 추가 확인  
  
### 작업 완료 후 검증  
- [x] 15개 도시 모두 `budget` 필드가 있는지 확인  
- [x] 15개 도시 모두 `environment` 배열이 최소 1개 이상 요소를 포함하는지 확인  
- [x] 15개 도시 모두 `bestSeason` 필드가 있는지 확인  
- [x] monthlyBudget과 budget 값이 일치하는지 확인  
- [x] TypeScript 컴파일 에러가 없는지 확인  
- [x] 웹페이지에서 카드 렌더링 시 에러가 없는지 확인  
  
### 주요 파일  
- [x] `lib/mock-data.ts` - 도시 데이터  
  
### 구현 가이드  
- [x] 각 도시 객체를 순회하며 새 필드 추가  
- [x] monthlyBudget 값에 따라 budget 자동 설정  
- [x] 도시별 특성을 고려하여 environment 배열 구성:  
  - [x] 자연친화: 제주, 강원, 경주, 통영 등  
  - [x] 도심선호: 부산, 대구, 대전 등  
  - [x] 카페작업: 강릉, 전주, 제주 등  
  - [x] 코워킹 필수: 세종, 대전 등  
- [x] 도시별 관광 특성을 고려하여 bestSeason 설정  
  
---  
  
## Phase 6: 로그인 상태 UI 개선 - 사용자 이메일 표시 및 드롭다운  
  
### 오버뷰  
로그인 성공 시 Header의 로그인/회원가입 버튼 대신 사용자 이메일을 우측 상단에 표시하고, 클릭하면 프로필 버튼과 로그아웃 버튼이 토글되는 드롭다운을 구현합니다.  
  
### 수정/개선 기능  
- [x] `components/layout/Header.tsx` 수정  
- [x] 로그인 상태일 때 이메일 표시 영역 개선:  
  - [x] 현재는 `<span>{user.email}</span>` 형태로 간단히 표시됨  
  - [x] 클릭 가능한 버튼으로 변경  
- [x] 이메일 클릭 시 드롭다운 메뉴 토글:  
  - [x] "프로필" 버튼 (링크 또는 버튼)  
  - [x] "로그아웃" 버튼  
- [x] 드롭다운 외부 클릭 시 메뉴 닫기  
- [x] Shadcn/ui의 DropdownMenu 컴포넌트 활용 고려  
- [x] 모바일에서도 동일한 동작 구현  
  
### 작업 완료 후 검증  
- [x] 로그인하지 않은 상태에서 로그인/회원가입 버튼이 표시되는지 확인  
- [x] 로그인 후 이메일이 우측 상단에 표시되는지 확인  
- [x] 이메일 클릭 시 드롭다운 메뉴가 나타나는지 확인  
- [x] 드롭다운에 "프로필", "로그아웃" 버튼이 있는지 확인  
- [x] 프로필 버튼 클릭 시 적절한 동작(페이지 이동 또는 모달)이 일어나는지 확인  
- [x] 로그아웃 버튼 클릭 시 로그아웃이 정상 작동하는지 확인  
- [x] 드롭다운 외부 클릭 시 메뉴가 닫히는지 확인  
- [x] 모바일에서도 동일하게 작동하는지 확인  
  
### 주요 파일  
- [x] `components/layout/Header.tsx` - 헤더 컴포넌트  
- [x] `components/ui/dropdown-menu.tsx` - Shadcn DropdownMenu (없으면 생성)  
- [x] `components/layout/UserMenu.tsx` - 사용자 드롭다운 메뉴 컴포넌트  
- [ ] `app/profile/page.tsx` - 프로필 페이지 (선택사항, 향후 구현)  
  
### 구현 가이드  
- [x] Shadcn/ui DropdownMenu 컴포넌트 설치 (아직 없다면):  
  ```bash  
  npx shadcn@latest add dropdown-menu  ```- [x] Header를 클라이언트 컴포넌트로 변경 (`'use client'`) - 이미 서버 컴포넌트이므로 드롭다운 부분만 별도 컴포넌트로 분리 고려  
- [x] `components/layout/UserMenu.tsx` 같은 클라이언트 컴포넌트 생성  
- [x] DropdownMenu 구조:  
  - [x] DropdownMenuTrigger: 사용자 이메일 버튼  
  - [x] DropdownMenuContent: 메뉴 내용  
  - [x] DropdownMenuItem: 프로필, 로그아웃 항목  
- [x] 로그아웃은 기존 logout action 활용  
- [x] 프로필 페이지는 Phase 6에서는 간단히 `/profile` 링크만 추가 (실제 구현은 향후)  
  
---  
  
## 전체 Phase 구현 순서 요약  
  
- [x] **Phase 1**: 별점 삭제, 좋아요/싫어요 버튼 인터랙션 구현  
- [x] **Phase 2**: 네비게이션 간소화  
- [x] **Phase 3**: 새로운 필터 타입 및 UI 구현  
- [x] **Phase 4**: 카드에 필터 정보 표시  
- [x] **Phase 5**: mock 데이터에 필터 속성 추가  
- [x] **Phase 6**: 로그인 상태 UI 개선  
  
각 Phase는 독립적으로 실행 가능하며, 순차적으로 진행하면 프로젝트가 점진적으로 개선됩니다.